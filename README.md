# Weak Spot Mining（弱点破壊 Mod）

Fortnite の資材集めにある「弱点（クリティカル）を叩くと速く壊せる」仕組みを Minecraft に持ち込む Mod です。

- 対応: **Minecraft Java Edition 1.12.2 / Forge 14.23.5.2860**
- 最新版: **1.0.5**（[Releases](https://github.com/zeusisgood/weakspot/releases)、変更点は[更新履歴](#更新履歴)）

## 遊び方

1. いつもどおりブロックを長押しで壊し始めると、狙っている面に**弱点（円いマーク）**が出ます。
2. 照準を弱点に重ねると**ヒット**します。ヒット音が鳴り、少しの間だけ破壊が一気に進みます。
3. ヒットすると、弱点は離れた場所へ移動します。追いかけて当て続けると、通常の約2倍の速さで壊せます。
4. 弱点に当てなくても、破壊は通常の速さで進みます。

補足:

- ツールは問いません（素手でも有効）。
- 長押しをやめても、弱点は約2秒残ります。別の面を狙うと、その面に出し直します。
- 岩盤などの壊せないブロック、一瞬で壊れるブロック、クリエイティブモードは対象外です。
- 弱点は自分の画面だけに表示され、他のプレイヤーとは共有しません。

## ヒット音

- **自分のヒット音**: ノートブロックの「プリング」の音です。当て続けるとドレミの音階で1オクターブ上がり、上がりきると最初の音に戻って繰り返します。連続ヒットは別のブロックに移っても続き、約2秒当てないと最初の音に戻ります。
- **他のプレイヤーのヒット音**（マルチプレイ）: 16ブロック以内にいる他のプレイヤーのヒット音も、叩かれたブロックの位置から聞こえます（離れるほど小さくなります）。
  - 自分とは別の楽器（初期値は木琴）で、小さめの音量で鳴ります。
  - 音階は、そのプレイヤーの連続ヒットに合わせて上がります。
  - 楽器と音量は、各自の設定（`othersHitSound` / `othersHitVolume`）で変えられます。音量を 0 にすると聞こえなくなります。バニラの「プレイヤー」音量も掛かります。

## 統計

ゲーム中に **K キー**で統計画面を開けます（キーは「設定 → 操作設定 → 弱点破壊」で変更できます）。

| 項目 | 内容 |
|---|---|
| 弱点ヒット数 | 弱点に当てた回数 |
| 壊したブロック数 | 弱点が出るブロックを壊した数（一瞬で壊れるブロックは数えない） |
| 弱点を当てて壊したブロック数 | そのうち、1回以上当ててから壊した数 |
| 1ブロックあたりの平均ヒット数 | 弱点ヒット数 ÷ 壊したブロック数 |
| 1ブロックでの最多ヒット数 | 1つのブロックで当てた回数の最大 |
| 短縮できた時間（推定） | ヒットで先に進んだ破壊を、通常速度の時間に換算した合計 |

- **今回**: ワールドやサーバーに入ってから出るまでの記録
- **累計**: 全ワールド・全サーバーの合計。`.minecraft/weakspot/stats.json` に保存されます。画面の「累計をリセット」を2回押すと消せます。

統計は自分のクライアントだけに記録され、サーバーや他のプレイヤーには送られません。

## インストール

### どこに入れるか

| 遊び方 | Mod を入れる場所 |
|---|---|
| シングルプレイ | 自分のクライアントだけ |
| LAN（ワールドを LAN に公開） | ホストと、参加する全員のクライアント |
| マルチプレイ（専用サーバー） | **サーバーと、参加する全員のクライアント** |

- サーバーとクライアントには、**同じバージョン**の jar を入れてください。
- 次の場合は接続が拒否されます。
  - 片方にしか入っていない
  - バージョンが違う
  - バニラ（Forge なし）のクライアントで接続した
- 弱点の表示とヒット判定は各クライアントが行い、破壊速度はサーバー側でも上げています。サーバーにも入れないと、壊したブロックが元に戻ります。

### 必要なもの

- Minecraft Java Edition **1.12.2**
- Minecraft Forge **1.12.2 - 14.23.5.2860**（[公式サイト](https://files.minecraftforge.net/net/minecraftforge/forge/index_1.12.2.html)から入手）
- Java 8（Forge 1.12.2 の要件）
- この Mod の jar（`weakspot-<バージョン>.jar`）。[Releases](https://github.com/zeusisgood/weakspot/releases) からダウンロードしてください。自分でビルドする場合は[開発](#開発)の手順を参照してください。

### クライアント（シングルプレイ・マルチプレイ共通）

1. Forge のインストーラー（`forge-1.12.2-14.23.5.2860-installer.jar`）を実行し、「Install client」でインストールする。
2. Minecraft ランチャーで Forge 1.12.2 のプロファイルを一度起動して閉じる（`mods` フォルダが作られる）。
3. jar を `mods` フォルダに入れる。
   - Windows: `%APPDATA%\.minecraft\mods`
   - macOS: `~/Library/Application Support/minecraft/mods`
   - Linux: `~/.minecraft/mods`
4. Forge 1.12.2 のプロファイルで起動する。タイトル画面の「Mods」に「Weak Spot Mining」があれば導入できています。

### 専用サーバー

1. サーバー用のフォルダで、Forge のインストーラーをサーバーモードで実行する。

   ```sh
   java -jar forge-1.12.2-14.23.5.2860-installer.jar --installServer
   ```

2. 一度起動して `eula.txt` を作り、[Minecraft EULA](https://aka.ms/MinecraftEULA) に同意する場合は `eula=true` に書き換える。

   ```sh
   java -Xmx2G -jar forge-1.12.2-14.23.5.2860.jar nogui
   ```

3. jar をサーバーの `mods` フォルダに入れて、もう一度起動する。
4. 参加するプレイヤーにも、同じバージョンの jar を配ってクライアントに入れてもらう。

### 更新するとき

古い jar を `mods` から削除してから、新しい jar を入れてください。サーバーとクライアントは同時に更新してください（バージョンが違うと接続できません）。
`config/weakspot.cfg` と統計（`.minecraft/weakspot/stats.json`）はそのまま引き継がれます。新しい設定項目は、次に起動したときに初期値で追加されます。

## 設定

初回起動後に生成される `config/weakspot.cfg` で変更できます（サーバーでは、サーバーフォルダの `config/` に作られます）。
設定はサーバーとクライアントの間で同期されません。

- **共通**の項目は、サーバーとクライアントの両方が使います。マルチプレイでは**サーバーと全員のクライアントで同じ値**にしてください。値がずれていると、壊したブロックが一度元に戻って見えることがあります。
- **クライアント**の項目は、見た目や音だけに関わるので、各自が好きな値にしてかまいません（サーバー側の値は使われません）。

### 共通

| キー | 初期値 | 説明 |
|---|---|---|
| `boostMultiplier` | 4.0 | ヒット時の破壊速度の倍率 |
| `boostDurationTicks` | 4 | 倍率を掛ける時間（tick） |
| `minHitIntervalTicks` | 6 | ヒットを受け付ける最小間隔（tick） |

### クライアント

| キー | 初期値 | 説明 |
|---|---|---|
| `weakSpotRadiusRatio` | 0.14 | 弱点の半径（面の短い辺に対する比率） |
| `edgeMargin` | 0.1 | 弱点の外周とブロック面の縁の間に空ける最小の距離（ブロック）。大きいほど弱点が中央に寄る |
| `minMoveDistance` | 0.4 | ヒット後に弱点が移動する最小距離（ブロック） |
| `lingerTicks` | 40 | 長押しをやめた後に弱点が残る時間（tick） |
| `othersHitVolume` | 0.4 | 他のプレイヤーのヒット音の音量（0〜1）。0 で聞こえなくなる |
| `othersHitSound` | XYLOPHONE | 他のプレイヤーのヒット音の楽器。`XYLOPHONE` `CHIME` `BELL` `FLUTE` `GUITAR` `HARP` `BASS` `HAT` `SNARE` `BASEDRUM` `PLING`（自分と同じ音）から選ぶ |

`edgeMargin` を大きくすると、弱点が動ける範囲が狭くなり、`minMoveDistance` だけ離れた場所を取れないことがあります。その場合、弱点はできるだけ遠い場所へ移動します。

### ブーストの目安

1回のヒットで、通常の `(倍率 − 1) × 継続時間` tick 分だけ破壊が進みます（初期値では 12 tick = 約0.6秒分）。約0.6秒ごとにヒットすると通常の約2倍の速さになり、`minHitIntervalTicks` の制限により最大で約3倍になります。

## 開発

JDK 8 が必要です。リポジトリの devcontainer を使うと、JDK 8 と Claude Code が入った環境で開発できます。

```sh
./gradlew build        # ビルドとテスト。成果物は build/libs/
./gradlew test         # 単体テストのみ
./gradlew runServer    # 開発用の専用サーバーを起動（run/ で起動。初回は run/eula.txt の同意が必要）
```

コンテナ内ではゲーム画面を出せないため、クライアント側の動作は、ビルドした jar をホスト側の Minecraft の `mods` に入れて確認します。

ソースは `com.example.weakspot` 以下にあります。

| パッケージ | 内容 |
|---|---|
| `common/` | Minecraft に依存しない計算（面の座標変換、弱点の配置、ブースト量、ヒット音の音階、統計の集計）。単体テストの対象 |
| `client/` | 弱点の状態管理、ヒット判定、描画、ヒット音（自分・他のプレイヤー）、統計画面 |
| `network/` | ヒット通知パケット（クライアント→サーバー、サーバー→周りのクライアント） |
| `server/` | サーバー側の破壊速度ブースト |
| `config/` | 設定 |

仕様は [SPEC_v1.0.md](SPEC_v1.0.md) にあります。設計上の注意点（サーバー側のブーストの仕組みなど）は [CLAUDE.md](CLAUDE.md) を参照してください。

## 更新履歴

### 1.0.5
- 他のプレイヤーのヒット音が聞こえるようになった（16ブロック以内。楽器と音量を設定で選べる。音階はそのプレイヤーの連続ヒットに合わせる）
- 通信内容が変わったため、1.0.4 以前とは接続できない。サーバーとクライアントを同時に更新すること

### 1.0.4
- ヒット音の音階が上がりきったら、最初の音に戻って繰り返すようにした

### 1.0.3
- 連続ヒットでヒット音の音階が上がるようにした

### 1.0.2
- 統計画面を追加（K キー）

### 1.0.1
- 設定 `edgeMargin` を追加。弱点がブロック面の縁に出ないようにした

### 1.0.0
- 最初のリリース
