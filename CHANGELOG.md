# 更新履歴

[← README に戻る](README.md)。版の決め方は [doc/development.md](doc/development.md#バージョンの方針)。

## 1.8.6

- **クリエイティブでも全ての弱点が出現**するように変更（採掘はブロックが即座に壊れるため対象外）。観戦モードでは出現しない。サーバーが 1.8.5 以前の場合は従来通り出現しない
- 内部構造を整理（ヒット受付の共通化、ヒット処理の振り分け表、統計・同期設定の保持方法、照準周辺の弱点の共通基盤、コンボ倍率）。遊び方に変更なし
- 通信内容・保存データ・設定に変更なし（統計と設定の送信内容が 1 バイトも変わらないことをテストで確認済み）。1.8.x 同士はそのまま接続可能

## 1.8.5

- ガイドの本を再構成: 表紙・**クリックで移動できる目次**・6 章構成に並べ替え、各ページを「■ 出るとき」「■ 当てると」「■ ヒント」の見出しで区切った。各ページ下部の「↩ 目次」で目次に戻れる。「照準のまわりの弱点」「弱点マーカー」「困ったとき」のページを追加
- 旧バージョンで入手した本は、ログイン時・開封時に新しい本へ自動で差し替え
- 通信内容・設定に変更なし。1.8.x 同士はそのまま接続可能（1.8.4 以前のクライアントでは、1.8.5 のサーバーで入手した本の文章が翻訳キーのまま表示される）

## 1.8.4

- 大人の村人に、しゃがんで素手で右クリック長押しすると、**繁殖できない理由**（村・ドア・待ち時間・食料・相手）をアクションバーに表示
- 動物の表の「繁殖の待ち時間」に、村人も対象であることを明記
- 通信内容・設定に変更なし（文章はサーバー側で生成して送信）。1.8.x 同士はそのまま接続可能

## 1.8.3

- K キーの画面の「弱点」タブを「**弱点マーカー**」にし、色と形を **◀ で 1 つ前に戻せる**ようにした
- 画面の言い回しを直した: 画面の題「弱点破壊」、統計の「採掘ヒット数」「最大コンボ」、HOME キーの「弱点（すべて）: オン／オフ」、採掘の節目「採掘 100 ヒット！」、エンチャントの注釈「マーカーに当てると…」、育たないときの知らせ、など
- クライアントだけの変更。通信内容は変わっていない。1.8.x 同士は、そのまま接続できる

## 1.8.2

- ボートで曲がると、弱点が元の向きに残って画面の外へ出てしまい、当てられなかったのを直した。弱点がボートの向きについて回る
- 照準のまわりの弱点（弓を含む）が、照準から 35 度より離れたら、今の照準の近くに出し直すようにした（大きく振り向いたときに見失わないように。ヒットには数えない）
- クライアントだけの変更。通信内容は変わっていない。1.8.x 同士は、そのまま接続できる

## 1.8.1

- 照準のまわりに出る弱点（はしご・走りなどの上下、近接の左右、弓・食事などの全方向）が、当て続けると少しずつずれて、真上・真下まで行ってしまうことがあったのを直した。次の弱点は前と反対側に出て、照準が水平から大きく離れたら水平に戻る側に出る（エリトラは反対側に出すだけ）
- クライアントだけの変更。通信内容は変わっていない。1.8.x 同士は、そのまま接続できる

## 1.8.0

- **近接の弱点を作り直した**。剣か斧を持って敵の近くにいる間、照準の左右に銀色の弱点が出て、当てるたびに溜まり、次に敵を殴るとクリティカル（1.5 倍）になって、さらに溜めた分だけ強くなる（1 回 ×1.25、コンボで上乗せ、初期値では上限なし）。溜めは持ち替えるまで残る。今までの「敵の体の弱点を、攻撃のゲージが溜まってから殴る」形はやめた
- 設定 `meleeWeakSpotScale` をなくし、`meleeChargePerHit` / `meleeChargeMax` / `meleeChargeBarEnabled` を足した。耐久回復は、溜めた攻撃が当たった回数で数える
- 統計の「クリティカル数」を「近接ヒット数」（弱点に当てた回数）にした（今までの数は引き継ぐ）
- **ネザーゲートの弱点**を追加した。ゲートの中に立っている間に弱点が出て、当てるたびに移動までの待ち時間が 1 秒ずつ縮む
- 統計に「ゲートヒット数」を足した
- 通信内容が変わったため、1.7.x とは接続できない。サーバーとクライアントを、1.8.0 に同時に更新すること

## 1.7.1

- 照準まわりの弱点（弓・釣り・乗り物・食事・はしご・エリトラ・投げる物・走り）と、睡眠・エンチャントのマーカーが、中空の丸（輪郭だけ）に見えていたのを直した。採掘の弱点と同じ塗りつぶしの丸になる（「弱点マーカー」タブで選んだ形も、塗られて見える）
- 新しい版に入れ替えて初めてワールドに入ったときに、チャットに更新のお知らせを出すようにした（各自の設定 `showUpdateNotes`）
- `/weakspot bug` を足した。報告の方法を出し、GitHub の報告の画面を、環境を書き込んだ状態で開ける（クリップボードにもコピーする）
- クライアントだけの変更。通信内容は変わっていない。1.7.x 同士は、そのまま接続できる

## 1.7.0

- **はしごの弱点**を追加した。はしご・ツタを登り降りしている間、照準の真上か真下に弱点が出て、当てると登り降りが速くなる（2 秒 1.5 倍、コンボで上乗せ、初期値では上限なし）
- **エリトラの弱点**を追加した。飛んでいる間に弱点が出て、当てた瞬間に見ている向きへ一気に飛び出す（トライデントの激流のような急加速。コンボで上乗せ、上限なし）
- **エンチャントの弱点**を追加した。エンチャント台の画面のマーカーをクリックで当てるたびに、3 つの候補が引き直される（何も減らない）
- **収穫の弱点**を追加した。実った作物を素手で右クリックしたままにすると弱点が出て、当てると収穫・植え直しされる。コンボで収穫物が増える
- **投げる物の弱点**を追加した。エンダーパール・雪玉などを持っている間に弱点が出て、当てるたびに溜まり、次に投げた物が速く遠くへ飛ぶ（持ち替えるまで残る）
- **走りの弱点**を追加した。走っている間、照準の真上か真下に弱点が出て、当てると速く走れる
- 節目を細かくした（採掘は 50 回から 10 万回まで 30 段階、その先は 5 万ごと）。採掘以外の**種類ごとの節目**と、**合計の節目**を足した。7 だけが並ぶ数は虹色で派手にした
- 統計画面に「**弱点**」タブを足した。弱点の種類ごとのオン・オフ（サーバーにも伝わる）と、自分の弱点の色（12 色から選ぶ、またはカラーコードを打ち込む）・形を変えられる
- 統計に、はしご・エリトラ・エンチャント・収穫・投げる物・走りのヒット数と、合計ヒット数を足した。統計の項目が入りきらないときは、ホイールで送れる
- 通信内容が変わったため、1.6.x とは接続できない。サーバーとクライアントを、1.7.0 に同時に更新すること

## 1.6.4

- 乗り物の加速の残り時間のゲージ（水色）を、照準の下から照準の上に移した（騎射などで、照準の下のコンボの表示と重なっていたため）
- クライアントだけの修正。通信内容は変わっていない。1.6.x 同士は、そのまま接続できる

## 1.6.3

- 頭の上のコンボ数が、しゃがんでいる人（機械を叩いている人など）に出なかったのを直した
- クライアントだけの修正。通信内容は変わっていない。1.6.x 同士は、そのまま接続できる

## 1.6.2

- 寝ている間の弱点のマーカーを、画面の中央のあたり（幅・高さとも 4 割の範囲）にだけ出すようにした（画面全体に出ると、次のマーカーが遠すぎたため）
- クライアントだけの変更。通信内容は変わっていない。1.6.x 同士は、そのまま接続できる

## 1.6.1

- 統計の累計をリセットすると、節目（採掘ヒット 100 / 777 / 1000 / 10000 回）をもう一度受け取れるようにした（統計画面の「累計をリセット」でも、`/weakspot reset` でも）。節目は、累計の採掘ヒット数で数える
- サーバーだけの変更。通信内容は変わっていない。1.6.x 同士は、そのまま接続できる

## 1.6.0

- 機械の弱点を叩いている間、コンボの下に**その機械の今の速さ**（「機械 4倍速」）を 1 ヒット目から出すようにした（今までは、コンボで上乗せされる掛け数だけを、コンボ 25 から出していた）。ディスペンサーの「発射 ×n」も、上限の設定まで考えた数になった
- 加速中の機械の粒子を、各自の設定（`machineParticlesVisible`）で見えなくできるようにした
- かまど・醸造台・モブスポナーを叩いている間、上に進み具合（黄）と燃料（橙）のバーを出すようにした
- 近くのほかのプレイヤーのコンボ（10 以上）を、頭の上に「n HIT」と出すようにした
- **乗り物の弱点**を追加した。馬・豚・トロッコ・ボートに乗って走っている間、照準の近く（馬・豚は真上か真下）に弱点が出て、当てると速くなる（2 秒 1.5 倍、コンボで上乗せ、初期値では上限なし）。馬・豚に乗って弓を引くと、弓の弱点も真上か真下に出て、当てると乗り物の加速も続く（騎射）
- **食事・飲み物の弱点**を追加した。食べている・飲んでいる間に弱点が出て、2 ヒットで食べ終わる
- **寝ている間の弱点**を追加した。夜にベッドで寝ている間、寝ている画面のマーカーをクリックすると、ワールドの時刻が進む（次の朝まで）
- 統計に、乗り物・食事・睡眠のヒット数を足した
- サーバーの版をクライアントに送るようにした（今後のパッチで足す機能を、古いサーバーでは出さないため）
- 通信内容が変わったため、1.5.x とは接続できない。サーバーとクライアントを、1.6.0 に同時に更新すること

## 1.5.5

- 近接の弱点が見える距離を、16 ブロックから 32 ブロックにした（当てられるのは、今までどおり攻撃が届く距離だけ）
- クライアントだけの変更。通信内容は変わっていない。1.5.x 同士は、そのまま接続できる

## 1.5.4

- 弓の弱点を、照準からもっと離れた所（視線から 10〜20 度）に出すようにした（今までは 3〜8 度で、照準をほとんど動かさずに当たった）。画面が狭いとき（弓を引き切ったとき、視野角の設定が小さいとき）は、画面からはみ出さないように近づける
- クライアントだけの変更。通信内容は変わっていない。1.5.x 同士は、そのまま接続できる

## 1.5.3

- 加速している機械のまわりに、色の粒を出すようにした。1 回目のヒットから出るので、すぐに加速していることが分かる。速いほど粒が多く、コンボで速さが上がると色が変わる（水色 → 黄 → 橙 → 赤 → ピンク → 紫 → 金）。近くのプレイヤー全員に見える。設定 `machineBoostParticles`（サーバーだけが使う）で止められる
- コンボの下の「機械 ×n」を「コンボボーナス ×n」にした（4 倍速に上乗せされる分で、機械の速さそのものではないため）
- 通信内容は変わっていない。1.5.x 同士は、そのまま接続できる

## 1.5.2

- ディスペンサー・ドロッパーの連射を、2 発目から 0.1 秒（2 tick）ごとに 1 発ずつ出すようにした（今までは同じ瞬間にまとめて出ていて、連射に見えなかった）
- ディスペンサー・ドロッパーを狙っている間、コンボの下に「発射 ×n」（1 回の信号で出る数）を出すようにした
- 通信内容は変わっていない。1.5.x 同士は、そのまま接続できる

## 1.5.1

- サーバーとクライアントの Mod の版が違うときに、ログインから約 2 秒後に知らせるようにした。本人には、どちらが古いかと更新のお願いを、OP 権限を持つほかのプレイヤーには誰が何の版で入ったかを、チャットで出す。サーバーのログにも出す。文章はサーバーが作るので、古いクライアントでも日本語・英語で読める
- 通信内容は変わっていない。1.5.x 同士は、そのまま接続できる（サーバーが 1.5.0 のままだと、知らせは出ない）

## 1.5.0

- 近接の弱点のマーカーを大きくした（初期値で約 1.5 倍）。大きさは新しい設定 `meleeWeakSpotScale`（サーバーの値）で変えられる。採掘・成長・機械・動物の弱点の大きさは変わらない
- 通信内容が変わったため、1.4.x とは接続できない。サーバーとクライアントを、1.5.0 に同時に更新すること

## 1.4.4

- 機械の加速の対象に、コンパレーター・リピーター・オブザーバー・ディスペンサー・ドロッパーを足した。叩くと、信号の部品は動くまでの待ち時間が縮み（クロック回路が速くなる）、ディスペンサー・ドロッパーは 1 回の信号で 2 発（コンボで最大 5 発）出る
- 通信内容は変わっていない。1.4.x 同士は、そのまま接続できる（1.4.3 以前のクライアントでは、これらのブロックに弱点が出ない）

## 1.4.3

- 作物・苗木の弱点に当てても育たないときに、チャットで知らせるようにした。暗いときは「暗くて育ちません（明るさ n）」、原因が分からないときは、30 回続けて変化がなければ「何らかの外部要因で育たないようです」と出る
- 設定 `growthWarnings` / `growthStuckHits`（サーバーだけが使う）を追加した
- 通信内容は変わっていない。1.4.x 同士は、そのまま接続できる（1.4.2 以前のクライアントでは、知らせが翻訳キーのまま出る）

## 1.4.2

- 機械の加速が続く時間の初期値を 4 → 6 tick にした。最短の間隔で当て続けると、途切れずに速く動く（1.4.1 以前の設定ファイルで 4 のままなら、自動で 6 に直す）
- コンボが続くほど、機械がさらに速くなるようにした（25 で ×1.25 から、1000 で ×4 まで。初期値なら 1000 コンボで 16 倍）。上限は新しい設定 `machineBoostMaxMultiplier`（サーバーだけが使う。初期値 16）
- 機械の弱点に照準を合わせている間、コンボの下に「機械 ×n」を出すようにした
- コンボの演出を 250、500、1000（以降 1000 ごと）まで延ばした。上の段階ほど派手になる（和音、駆け上がり、花火、「1000 COMBO!」のタイトル）。虹色も段階ごとに速く流れる
- 通信内容は変わっていない。1.4.x 同士は、そのまま接続できる

## 1.4.1

- 遊び方のガイドの本を追加した。初めてワールド（サーバー）に入ったときに1冊もらえる（1.4.0 以前から遊んでいる人も、1回もらえる）。文章は、各自の言語（日本語・英語）で出る。統計画面の「ガイド」ボタンでも読める
- 設定 `giveGuideBook`（サーバーだけが使う）で、本を渡さないようにできる
- 通信内容は変わっていない。1.4.x 同士は、そのまま接続できる

## 1.4.0

- 成長の弱点の追加リスト（`growthExtraBlocks`）を、汎用の書き方にした。どの Mod のブロックでも、登録名（と、必要なら `[age=0-6]` のような状態の条件）を書けば、成長の弱点の対象になる（当てると、ランダムな tick を余分に呼んで早める）。今までの対象（サトウキビ・サボテン・ネザーウォート・IC2 のゴムの木）は、今までどおり動く
- 1マス全体のブロック（IC2 のゴムの木の幹など）の成長の弱点は、照準を当てた面に出すようにした
- 既知の問題: 成長バーは、`age` か `stage` という数の状態を持つブロックにだけ出る（IC2 のゴムの木などには出ない）
- 設定 `growthExtraBlocks` の意味が変わった（今までは、コードで決めたブロック以外は無視した）ため、1.3.x とは接続できない。サーバーとクライアントを、1.4.0 に同時に更新すること

## 1.3.7

- IC2 のゴムの木の、樹液を取ったあとの乾いた穴にも、成長の弱点が出るようにした（素手で右クリックを押しっぱなし。穴のある面に出る）。当てると穴の再生が早まる（1 ヒットで約半分の確率で戻る）
- 設定 `growthExtraBlocks` の初期値に `ic2:rubber_wood` を足した。既存の設定ファイルにも、サーバーの起動時に1回だけ足す（`configVersion=3`。そのあと取り除けば、対象外のまま）
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる（ゴムの木に弱点を出すには、サーバーとクライアントの両方を 1.3.7 にする）

## 1.3.6

- 他のプレイヤーが機械の加速や作物の成長の弱点に当てているとき、そのマークが見えるようにした（今までは採掘と動物だけだった）
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる（叩いている人のクライアントが 1.3.6 なら、見る側は 1.3.x のどれでも見える）

## 1.3.5

- 近接の弱点が、16 ブロック先の敵から見えるようにした（今までは攻撃が届く距離だけだった）。クリーパーなども、近づく前に弱点の位置を確かめられる。当てられるのは、今までどおり攻撃が届く距離だけ
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる（クライアントを 1.3.5 にすると見える）

## 1.3.4

- 近接の弱点でクリティカルにすると、5 回ごとに手に持っている物の耐久が 1 回復するようにした（`critsPerRepair` / `critRepairPerStep`。サーバーだけが使う）
- 弓を引き切ったあとも弱点が出るようにした（過剰チャージ）。当てるたびに放つ矢のダメージが +10%、5 ヒット（+50%）まで上がる。ゲージの下に赤い目盛りが出る
- 弱点の一時オフのキーの初期値を、J から HOME にした（JourneyMap と重なるため）。すでに遊んでいた人は J のままなので、操作設定で変えるか「リセット」を押す
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる

## 1.3.3

- 他のプレイヤーが動物・釣り・弓・近接の弱点に当てたときも、ヒット音が聞こえるようにした（これで、すべての種類で聞こえる）。鳴る位置は、動物・近接は当てた生き物、釣りは浮き、弓は弓を引いているプレイヤー
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる（サーバーを 1.3.3 にすると聞こえる）

## 1.3.2

- 他のプレイヤーが機械の加速や作物の成長の弱点に当てたときも、ヒット音が聞こえるようにした（今までは採掘だけだった）
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる（サーバーを 1.3.2 にすると聞こえる）

## 1.3.1

- 近接の弱点が足先に出て狙いにくかったので、敵の頭・首・胴体のあたり（当たり判定の箱の、上の 6 割）にだけ出るようにした。見下ろしたときの頭のてっぺんには出る。見上げたとき（底面）には出ない
- 通信内容は変わっていない。1.3.x 同士は、そのまま接続できる

## 1.3.0

- 弓に弱点を追加した。弓を引いている間、照準の近くに弱点が出て、照準を合わせると弓の引きが早く進む（1ヒット 5 tick 分。他の Mod の弓も対象）
- 弓を引いている間、照準の下に引き具合のゲージを出すようにした（`bowDrawBarEnabled` でオフにできる）
- 敵に弱点を追加した。攻撃が届く距離で敵に照準を合わせると弱点が出て、攻撃のゲージが溜まった状態で弱点を殴ると、クリティカルヒット（ジャンプ攻撃と同じ 1.5 倍）になる
- 統計画面に、「弓ヒット数」と「クリティカル数」を追加した
- 動物の弱点の透かし（`animalSpotSeeThrough`）が、敵の弱点にも効くようにした
- 通信内容が変わったため、1.2.x とは接続できない。サーバーとクライアントを、1.3.0 に同時に更新すること

## 1.2.4

- キノコが巨大キノコに育つ確率の初期値を 10% から 20% にした（`mushroomGrowChance`）。設定ファイルの値が古い初期値 0.1 のままなら、サーバーの起動時に 0.2 に書き換える（ほかの値に変えていれば、そのまま）
- 通信内容は変わっていない。1.2.x 同士は、そのまま接続できる

## 1.2.3

- 設定を読み直すコマンド `/weakspot reload` を追加した（OP のみ）。専用サーバーでも、`weakspot.cfg` を書き換えた後に再起動せずに反映できる
- 通信内容は変わっていない。1.2.x 同士は、そのまま接続できる

## 1.2.2

- キノコにも成長の弱点が出るようにした。当てるたびに一定の確率（設定 `mushroomGrowChance`、初期値 10%）で巨大キノコに育つ（骨粉1回と同じ処理。暗い場所か菌糸・ポドゾルの上で、上に空きが必要なのはバニラと同じ）
- 設定 `growthExcludedBlocks` の初期値から、キノコを外した。1.2.1 以前に作られた設定ファイルからも、サーバーの初回起動時に1回だけキノコを取り除く（その後に書き戻せば、対象外のまま）。移行が済んだかは、新しい設定 `configVersion` で覚える（書き換えないでください）
- 通信内容は変わっていない。1.2.x 同士は、そのまま接続できる（キノコに弱点が出るかは、サーバーの設定で決まる）

## 1.2.1

- ニワトリなど、体が当たり判定より大きい動物で、自分の弱点が体に隠れて見づらかったのを直した。隠れた部分も薄く透けて見える（設定 `animalSpotSeeThrough`。他のプレイヤーのマークは透かさない）
- 通信内容は変わっていない。1.2.x 同士は、そのまま接続できる

## 1.2.0

- ボタンやハーフブロックなど、小さいブロックでも、弱点を当てやすく、動くようにした
- 面が極端に小さい（カーペットの側面など）ときは、弱点を出さないようにした
- 弱点の最小の大きさなどを、設定で変えられるようにした（`weakSpotMinRadius`、`weakSpotMaxRadiusRatio`、`minFaceSize`。サーバーの値が使われる）
- 成長促進の追加リストを、設定で変えられるようにした（`growthExtraBlocks`。サーバーの値が使われる）
- 統計画面に、「最大連続ヒット数」を追加した（今回・累計）
- 動物にも弱点が出るようにした。子どもの成長、繁殖の待ち時間、羊毛の再生、ニワトリの卵、村人の取引上限のリセットを、弱点に当てて早められる（素手で右クリックを押しっぱなし。馬・村人などは、しゃがみが必要）。足元に進み具合のバーが出る
- 釣りに弱点を追加した。浮きのまわりに出る弱点を、釣り竿を持って左クリックで叩くと、魚が寄ってくるまでの待ち時間が短くなる
- 統計画面に、「動物ヒット数」と「釣りヒット数」を追加した
- J キーで弱点をオフにしたとき、サーバーも右クリックの抑止を止めるようにした（オフのとき、しゃがみ右クリックで機械の GUI が開く）
- 通信内容が変わったため、1.1.x とは接続できない。サーバーとクライアントを、1.2.0 に同時に更新すること

## 1.1.6

- 成長の弱点を出している作物の足元に、成長の進み具合のバーを表示するようにした
- 他のプレイヤーの弱点マークの形を、設定で変えられるようにした（初期値は中抜きの輪）
- J キーで、自分の弱点をオン・オフできるようにした（建築中など、弱点が邪魔なとき用）
- 管理コマンド `/weakspot stats` と `/weakspot reset` を追加した（OP のみ。オンラインのプレイヤーが対象）
- 通信内容は変わっていない。1.1.x 同士は、そのまま接続できる

## 1.1.5

- 掘っているブロックの残りの耐久を、面の下のほうに緑のバーで表示するようにした
- 設定 `blockHealthBarEnabled` で、オフにできる
- 通信内容は変わっていない。1.1.x 同士は、そのまま接続できる

## 1.1.4

- 弱点が移動するとき、古い位置から新しい位置へ、残像を引いて素早く動くようにした（目で追いやすくするため。見た目だけで、当たり判定は移動先で即時）
- 他のプレイヤーの弱点マークにも、同じ演出が出る
- 設定 `weakSpotTrailEnabled` で、オフにできる
- 通信内容は変わっていない。1.1.x 同士は、そのまま接続できる

## 1.1.3

- 連続ヒットの数（コンボ）を、画面に表示するようにした。ヒット音の音階と同じ条件（約2秒ヒットがないと途切れる）で数える
- コンボの数の色が、段階（10 / 25 / 50 / 100）で変わり、節目で短い演出が出る
- 表示の位置・大きさ・オンオフは、設定で変えられる
- 死亡したとき、ディメンションを移動したときにも、ヒット音の音階が最初の音に戻るようにした（コンボと同時に戻る）
- 通信内容は変わっていない。1.1.x 同士は、そのまま接続できる

## 1.1.2

- サトウキビ、サボテン、ネザーウォートにも、右クリックを押しっぱなしにして弱点を当てると、成長を促進できるようにした
- 育っても意味がないブロック（草ブロック、背の高い草、キノコなど）には、成長の弱点が出ないことを確かめた（1.1.0 から、設定 `growthExcludedBlocks` の初期値で外している）
- 同じ大きさの側面がいくつもある植物（サトウキビ、サボテン、ココアの一部の段階）では、照準が当たっている側面に弱点を出すようにした
- 通信内容は変わっていない。1.1.x 同士は、そのまま接続できる

## 1.1.1

- 弱点に当てるだけで、ブロックを壊さなくても耐久が回復し続ける不具合を直した。ブロックを壊したときにだけ、回復が確定する（壊すたびに回復できる耐久の上限は、設定 `maxRepairPerBreak`）
- 通信内容は変わっていない。1.1.0 のサーバー・クライアントと、そのまま接続できる

## 1.1.0

- 統計と設定を、サーバー側で管理するようにした（統計はワールド・サーバーごと。設定はサーバーの値が正）
- 統計画面（K キー）に、ヒット音の楽器・音量の変更と試聴、Forge の設定画面を開くボタンを追加した
- 5ヒットごとに、手に持っているツールの耐久が1回復するようにした（設定で変えられる）
- 100 / 777 / 1000 / 10000 ヒットの節目で、祝いの演出と、少量の経験値・耐久回復を追加した
- 作物・苗木に、右クリックしっぱなしで弱点が出て、当てると成長が促進されるようにした
- 機械に、しゃがんで右クリックしっぱなしで弱点が出て、当てると加速されるようにした（機械 MOD によっては不具合の原因になることがある。ワールドのバックアップを取ってから使うこと）
- 他のプレイヤーの弱点マークが、別の色で見えるようにした
- 自分のヒット音にも、バニラの「プレイヤー」の音量が掛かるようにした
- 1.0.x の統計（`stats.json`）は引き継がない
- 通信内容が変わったため、1.0.x とは接続できない。サーバーとクライアントを、1.1.0 に同時に更新すること
- 今後は、同じ 1.1.x の間は、パッチ違いでも接続できる（バージョンの方針を参照）

## 1.0.5
- 他のプレイヤーのヒット音が聞こえるようになった（16ブロック以内。楽器と音量を設定で選べる。音階はそのプレイヤーの連続ヒットに合わせる）
- 通信内容が変わったため、1.0.4 以前とは接続できない。サーバーとクライアントを同時に更新すること

## 1.0.4
- ヒット音の音階が上がりきったら、最初の音に戻って繰り返すようにした

## 1.0.3
- 連続ヒットでヒット音の音階が上がるようにした

## 1.0.2
- 統計画面を追加（K キー）

## 1.0.1
- 設定 `edgeMargin` を追加。弱点がブロック面の縁に出ないようにした

## 1.0.0
- 最初のリリース
