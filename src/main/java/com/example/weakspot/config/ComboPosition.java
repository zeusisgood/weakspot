package com.example.weakspot.config;

/** コンボの表示の位置（各自の設定。描画はクライアント側の ComboHud）。 */
public enum ComboPosition {
    /** 照準の少し下。 */
    BELOW_CROSSHAIR,
    /** 照準の右。 */
    RIGHT_OF_CROSSHAIR,
    /** 画面の上の中央（ボスバーがあれば、その下）。 */
    TOP_CENTER
}
