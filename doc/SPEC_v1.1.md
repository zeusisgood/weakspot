# 弱点破壊 Mod 追加仕様書（v1.1.0）

`SPEC_v1.0.md` および現行実装（Mod 1.0.5）に対する**追加・変更**をまとめる。ここに書かれていないことは、`SPEC_v1.0.md` と現行実装のままとする。
仕様と食い違う実装をする場合は、ユーザーに確認すること。

この仕様書の内容は、すべて Mod のバージョン **1.1.0** として、まとめてリリースする（§0）。

- 対象: Minecraft Java Edition 1.12.2 / Forge 14.23.5.2860
- 追加内容: サーバー集約（統計・設定）、節目の演出と報酬、耐久回復、作物・苗木の成長促進、機械の加速、ヒット音の変更と試聴、他のプレイヤーの弱点マークの表示

---

## 0. バージョン（v1.1.0）

- この仕様書に書かれたすべての機能を、Mod のバージョン **1.1.0** として、**まとめて**リリースする（現行は 1.0.5）
- 1.1.0 に含める機能:
  1. 統計と設定のサーバー集約（§2、§3）
  2. 統計画面のサウンドの部品（ヒット音の変更と試聴。§3.5）
  3. 耐久回復（5ヒットごと。§4）
  4. 節目の演出と報酬（§5）
  5. 作物・苗木の成長促進（§6）
  6. 機械の加速と、右クリックの共通ルール（§7、§8）
  7. 他のプレイヤーの弱点マークの表示（色の変更まで。§8.5）
- 1.1.0 に含めないもの: §14 のスコープ外
- 通信内容が変わるため、1.0.x 以前とは接続できない。サーバーと全員のクライアントを、1.1.0 に同時に更新する
- バージョンを書き換える場所は、`build.gradle` の `version` と `WeakSpotMod.VERSION` の2か所。両方を `1.1.0` にそろえる
- **1.1.0 は、出すまで、開発の途中でバージョンを上げない**
- **1.1.0 以降のバージョンの方針**:
  - 機能の追加や、不具合の修正ごとに、**パッチ**を上げる（1.1.0 → 1.1.1）
  - 互換性を破るときに、**マイナー**を上げる（1.1.x → 1.2.0）。迷ったら、マイナーを上げる
  - 互換性を破る変更とは、次のどれかに当てはまるもの
    - 通信内容の変更（パケットの追加、削除、中身の変更）
    - サーバーが保存するデータの形式の変更で、古い版では読めなくなるもの（統計の保存形式など）
    - 設定キーの削除や、意味の変更
  - 通信内容を変えたら、必ずマイナーを上げる（パッチ違いでも接続できるようにするため）
- **接続できる範囲**: `@Mod` の `acceptableRemoteVersions` を、同じ 1.1.x 同士なら接続できる範囲にする（例: `[1.1,1.2)`。書式は実装時に確認する）。今は未指定で、パッチが1つ違うだけでも接続が拒否される。マイナーを上げるときは、この範囲も新しいマイナーに書き換える
- `CLAUDE.md` のリリースの流れにある「機能ごとにパッチバージョンを上げる」は、上の方針に書き換えること。あわせて、`acceptableRemoteVersions` の扱いと、マイナーを上げるときの手順も、`CLAUDE.md` に書くこと
- リリースの流れ（`CLAUDE.md` の流れと同じ）: README の「最新版」の行と「更新履歴」に 1.1.0 を追加 → コミット → 注釈付きタグ `v1.1.0` → `main` とタグを push。GitHub Release は、ユーザーが手動で作り、`build/libs/weakspot-1.1.0.jar` を添付する
- この仕様書のファイル名 `SPEC_v1.1.md` は、Mod 1.1.0 に対応する。`CLAUDE.md` の「仕様の正本」に、この仕様書を足すこと

---

## 1. 変更の全体像

| 項目 | 現行（1.0.5） | 変更後 |
|---|---|---|
| 統計 | クライアントが記録（`stats.json`、全ワールド合計） | **サーバーが記録**（ワールド・プレイヤーごと） |
| 設定 | 同期なし（各自の `weakspot.cfg`） | **サーバーの値が正**（接続時にクライアントへ送る） |
| ヒット数の数え方 | クライアントの統計のみ | サーバーが受け付けた検証済みヒットを数える |
| 弱点の対象 | 左クリックで壊すブロック | 加えて、**右クリックしっぱなし**で、作物・苗木と機械 |
| 他のプレイヤーの弱点 | 見えない | **見える**（表示のみ。当たり判定はなく、弱点そのものは各自のまま。§8.5） |

## 2. ヒットの数え方と統計（サーバー集約）

### 2.1 数え方
- サーバーが受け付けた**検証済みのヒット**を、プレイヤーごとに数える
- ヒットは3種類に分けて別々に数える
  - **採掘ヒット**（左クリック。従来のヒット）
  - **成長ヒット**（作物・苗木。§6）
  - **機械ヒット**（機械の加速。§7）
- 節目（§5）と耐久回復（§4）の対象は、**採掘ヒットだけ**

### 2.2 保存
- ワールド（サーバー）のセーブデータの中に、プレイヤーごとに保存する
- シングルプレイでも、ワールドごとに記録する
- 死亡してもリセットされないこと
- 全ワールド・全サーバーの合計は出せなくなる。既存の `stats.json` は使わない（引き継ぎはしない）

### 2.3 統計画面（K キー）
- サーバーから数字をもらって表示するだけの画面にする（画面を開いたときにサーバーへ要求する）
- 表示項目:
  - 弱点ヒット数（採掘ヒット）
  - 壊したブロック数（弱点が出るブロックを壊した数。一瞬で壊れるブロックは数えない）
  - 弱点を当てて壊したブロック数
  - 1ブロックあたりの平均ヒット数
  - 1ブロックでの最多ヒット数
  - 短縮できた時間（サーバーが受け付けたヒットの追加進捗を、通常速度の時間に換算した合計）
  - **成長ヒット数**（新規）
  - **機械ヒット数**（新規）
- 「今回」（ワールドやサーバーに入ってから出るまで）と「累計」（そのワールド・サーバーの合計）を分けて出す
- 「累計をリセット」は残す（サーバーの記録をリセットする。誤操作防止に2回押しは維持）
- 「壊した」の判定は、サーバーの破壊イベントで行う（クライアントの推定は使わない）
- 他のプレイヤーの統計は見られない。自分の分だけ表示する
- 画面に、サウンドの部品（§3.5）と、Forge の設定画面を開くボタンを置く

## 3. 設定の同期（サーバーが正）

### 3.1 ルール
- サーバー（専用サーバー、またはシングルプレイ・LANのホスト）の設定値が正
- プレイヤーがログインしたとき、サーバーは設定値を、そのクライアントへ送る
- クライアントは、接続している間はサーバーの値を使い、切断したら自分の設定値に戻る
- サーバーの設定が変わったら、接続中の全員へ再送する
- 専用サーバーでは、設定ファイル（`weakspot.cfg`）を書き換えて再起動する
- シングルプレイ・LANのホストは、Mods メニューで変えられる（変えたら再送する）

### 3.2 サーバーの値に従う項目

| 項目 | 内容 |
|---|---|
| `boostMultiplier` / `boostDurationTicks` / `minHitIntervalTicks` | 破壊速度のブースト（従来の共通項目） |
| `weakSpotRadiusRatio` | 弱点の大きさ |
| `edgeMargin` | 縁からの余白 |
| `minMoveDistance` | 弱点の最小移動距離 |
| `lingerTicks` | 長押しをやめた後に弱点が残る時間 |
| 新規項目すべて（§9） | 耐久回復、節目、成長、機械、対象外リスト、他のプレイヤーのマークの転送範囲 |

### 3.3 各自の設定のまま（サーバーの値は使わない）

| 項目 | 内容 |
|---|---|
| `othersHitVolume` | 他のプレイヤーのヒット音の音量 |
| `othersHitSound` | 他のプレイヤーのヒット音の楽器 |
| `myHitSound`（新規） | 自分のヒット音の楽器。初期値は現行と同じ `PLING`。選べる一覧は `othersHitSound` と同じ |
| `myHitVolume`（新規） | 自分のヒット音の音量。初期値は現行の音量と同じ |
| `otherMarkerEnabled`（新規） | 他のプレイヤーの弱点マークを表示するか。初期値は `true` |
| `otherMarkerColor`（新規） | 他のプレイヤーの弱点マークの色。初期値は `#3FA9FF`（水色。自分のマークの色を確認して、区別しにくければ変える。§15） |
| `otherMarkerAlpha`（新規） | 他のプレイヤーの弱点マークの透明度。初期値は、自分のマークより少し薄い値 |

- `myHitSound` と `myHitVolume` は、採掘・成長・機械のすべてのヒット音に使う
- 音階が上がる仕組み（1オクターブで最初に戻る、約2秒ヒットがないとリセット）は、今のまま変えない
- 音階の種類や、繰り返しのオン・オフなど、音階の上がり方は変えられなくてよい（§14）
- 変える場所: ヒット音の楽器と音量は、統計画面のサウンドの部品（§3.5）でも、Forge の設定画面でも変えられる。マークの表示・色・透明度は、Forge の設定画面で変える

### 3.4 Mods メニュー
- 専用サーバーに接続している間は、メニューで変えた値が使われない。その旨を、メニューに表示する
- ただし、§3.3 の各自の設定（ヒット音の楽器と音量、他のプレイヤーのマークの表示）は、接続中も常に使われる

### 3.5 統計画面のサウンドの部品（新規）

統計画面（K キー）に、ヒット音の楽器と音量を変えて試聴するための部品を置く。あわせて、Forge の設定画面を開くボタンを置く。

**方針**
- 現行の設定画面は、Forge が `@Config` から自動で作っており、ボタンなどを足せない。また、値を変えている途中は検出できず、「完了」を押したときにイベントが来るだけになる。そのため、聞いてから決められる試聴は、Mod 自身の画面である**統計画面**に置く
- 画面の種類は増やさない
- 値は、今の `weakspot.cfg`（§3.3 の項目）にそのまま保存する。統計画面で変えても、Forge の設定画面で変えても、同じ値になる
- 統計画面の窓が窮屈な場合は、同じ画面の中で「統計」と「サウンド」をタブで切り替えてよい（実装で判断する）

**部品**

| 部品 | 動き |
|---|---|
| 自分のヒット音の楽器 | 切り替えボタン。押すと次の楽器になり、切り替えた瞬間に1音鳴らす |
| 自分のヒット音の音量 | スライダー。動かし終えたときに、1音鳴らす |
| 自分の試聴ボタン | 押すと、実際のヒットと同じ音階を、最初から最後まで自動で鳴らす |
| 他のプレイヤーの楽器・音量・試聴 | 自分と同じ。試聴は、距離なしで鳴らす（実際は距離で小さくなる） |
| 「設定画面を開く」ボタン | 今の Forge の設定画面を開く。閉じると、統計画面に戻る。戻ってきたときは、値を読み直して、部品に反映する |

**ルール**
- 試聴の音にも、バニラの「プレイヤー」の音量設定が掛かる
- サウンドの部品は、クライアントだけで完結し、サーバーとは通信しない。統計がサーバーからまだ届いていなくても、使える
- 専用サーバーに接続している間も、変えられる（各自の設定なので）
- 他のプレイヤーの弱点マークの表示・色・透明度（§8.5）は、Forge の設定画面で変える。試聴は要らないので、統計画面には置かない
- 「設定画面を開く」ボタンで、Forge の設定画面へ飛べない場合は、自前で設定画面を組み立てて開く方法に切り替える（§12）

## 4. 耐久回復（5ヒットごと）

- 採掘ヒットの累計が `hitsPerRepair` の倍数になったとき、手に持っているツール（メインハンド）の耐久を `repairPerStep` 回復する
- 初期値: 5ヒットごとに1回復
- 耐久のないもの（素手など）や、耐久が減っていないものは、回復しない（累計は数える）
- 処理はサーバー側で行う

## 5. 節目の演出と報酬

### 5.1 節目
- 初期値: 100 / 777 / 1000 / 10000（設定で変えられる）
- 採掘ヒットの累計が、節目の数字に達した瞬間に、1回だけ発火する
- 節目の数え方は、サーバーが記録したそのワールド・プレイヤーの累計

### 5.2 報酬（サーバー側）
- 少量の経験値を渡す（節目ごとに、設定で量を変えられる）
- 手に持っているツールの耐久を、少し回復する（節目ごとに、設定で量を変えられる）
- ヒットごとの経験値は**なし**（経験値は節目のときだけ）

### 5.3 演出（クライアント側）
- サーバーが、達成したプレイヤーのクライアントへ、節目の通知を送る
- 通知を受けたクライアントが、次の演出を出す
  - 画面中央に、大きな祝いメッセージ
  - チャットに1行
  - 花火のパーティクル
  - 音（既存のヒット音の音階を、節目で一気に駆け上がらせる）
- **777** は、他の節目より派手にする（金色や虹色の文字など）
- 演出はMVPでは簡素でよい。原作寄せの凝った演出は後から追加する
- 達成した本人にだけ出す（他のプレイヤーへの発表はしない）

## 6. 作物・苗木の成長促進

### 6.1 操作
- **素手**（メインハンドが空）で、作物・苗木に向かって**右クリックを押しっぱなし**にすると、弱点が出る
- 弱点に照準が重なった瞬間が、ヒット。ヒットしたら、弱点は前回の位置から離れた場所へ移動する
- 弱点の位置はプレイヤーごとにローカルで計算する。他のプレイヤーと共有するのは、見た目の表示だけ（採掘と同じ。§8.5）

### 6.2 対象
- 成長できる状態の `IGrowable` を持つブロック（作物、苗木など）
- すでに完全に育っているもの、成長の条件を満たさないものには、弱点を出さない
- 登録名で対象外にするブロックを、設定で指定できる（`excludedBlocks` と別に、成長用の対象外リストを持つかは、実装時に判断してよい）

### 6.3 効果（サーバー）
- ヒットのたびに、そのブロックに対して `randomTick` を `growthTicksPerHit` 回、余分に呼ぶ
- 骨粉のように一気に育てるのではなく、元の成長条件（明るさ、農地の水分など）を守ったまま速くする
- 初期値: 1ヒットあたり5回（仮置き。遊びながら調整）

### 6.4 サーバーでの確認
- そのブロックに、直前に右クリックしたか（一定tick以内）
- 手が届く距離か
- ヒットの間隔（`growthMinHitIntervalTicks`）
- 対象のブロックか

### 6.5 弱点の位置
- 作物は高さが低く、側面が細い。**一番大きい面（多くは上面）にだけ**弱点を出す
- 弱点の半径は、その面の短い辺に対する比率で決める（採掘と同じ）。小さすぎる場合は、最小の半径を設ける

### 6.6 統計
- 成長ヒットは、統計の「成長ヒット数」に数える。採掘ヒットの累計、節目、耐久回復には含めない

## 7. 機械の加速

### 7.1 方式
Random Things の「Time in a Bottle」と同じ方式にする。タイルエンティティの `update()` を、余分に呼ぶ。

### 7.2 操作
- **しゃがんで、素手のまま**、機械のブロックに向かって右クリックを押しっぱなしにすると、弱点が出る
- 弱点に照準が重なった瞬間が、ヒット。ヒットしたら、弱点は移動する

### 7.3 対象
- `ITickable` を実装したタイルエンティティを持つブロック
- **許可リストは作らない**。対象外にするブロックを、設定（`excludedBlocks`）で指定する
- 初期の対象外: 宝箱、トラップチェスト、エンダーチェスト、エンチャントテーブル、ビーコン（加速しても意味がないバニラのブロック。GUI も通常どおり開く）

### 7.4 効果（サーバー）
- ヒットしたら、サーバーが、そのブロックの位置と残りtickを記録する（エンティティは使わない。セーブデータに残さない）
- 記録がある間、毎tick、`update()` を `(倍率 - 1)` 回、余分に呼ぶ
- 初期値: 倍率 4.0 を 4 tick（採掘のブーストと同じ値。ただし設定は別）
- 複数のプレイヤーが同じ機械を加速しても、効果は足さず、**大きいほうだけ**を使う
- 機械の電力やアイテムの消費は、機械の作りに任せる（加速しても、電力が足りなければ速くならない）

### 7.5 サーバーでの確認
- しゃがんでいるか、両手が空か
- そのブロックに、直前に右クリックしたか
- 手が届く距離か
- ヒットの間隔（`machineMinHitIntervalTicks`）
- 対象のブロックか

### 7.6 リスク（承知のうえで実装する。README に書く）
- 機械 MOD は、`update()` が「1tickに1回」だけ呼ばれる前提で作られているものが多い。余分に呼ぶと、不具合やクラッシュの原因になることがある
- 実際に、Time in a Bottle でも、機械の電力が足りない状態で、例外が出た報告がある
- `update()` を持たず、予約されたtick（スケジュールtick）で動くブロックは、この方式では加速できない

### 7.7 統計
- 機械ヒットは、統計の「機械ヒット数」に数える。採掘ヒットの累計、節目、耐久回復には含めない

## 8. 右クリックの共通ルール

- 弱点が出ている間（§6、§7 の対象ブロックを、条件どおりに右クリックしっぱなしにしている間）は、そのブロックの**通常の右クリック動作**（GUI を開く、右クリックで収穫する、など）を止める
- 機械（§7）は、素手でしゃがんで右クリックしたときに、GUI を**開かない**。しゃがまずに右クリックすれば、今までどおり GUI が開く
- 何かを持っている場合は、今までどおりの動作（設置、使用など）のまま
- 対象外のブロック（§7.3）は、しゃがみ右クリックでも、GUI が通常どおり開く
- 通常の右クリック動作を止める処理は、クライアントとサーバーの両方で行う

## 8.5 他のプレイヤーの弱点マークの表示（新規）

他のプレイヤーが長押しで叩いているブロックの弱点マークを、自分の画面にも出す。見えるだけで、当たり判定はない。弱点そのものは、今までどおり各自のもので、共有しない。

### 8.5.1 表示
- 他のプレイヤーの弱点マークを、自分のマークとは**明確に違う色**で描く（初期値は水色 `#3FA9FF`。§3.3 の設定で変えられる）
- 透明度は、自分のマークより少し薄くする（設定で変えられる）
- 自分の位置から、`markerShareRange` を超えて離れたマークは描かない
- 形は、今回は自分のマークと同じ（円）。**形の変更は、後のバージョンで追加する**（中抜きの輪、ひし形、四角など。§14）

### 8.5.2 通信（案）
- クライアントは、自分の弱点が「出たとき」「動いたとき」「消えたとき」だけ、その状態をサーバーへ送る
- サーバーは、それを、`markerShareRange` 以内にいる他のプレイヤーへ転送する（ヒット音の転送と同じ流れ。既存の `OtherHitMessage` の仕組みを参考にしてよい）
- 送る中身: どのプレイヤーの、どのブロックの、どの面の、面の中のどの位置か。大きさは全員同じ設定値なので送らない
- 弱点の位置は、クライアントが決めた値のまま、サーバーは検証せずに転送する（他のプレイヤーには見えるだけで、破壊やヒットには影響しないため）
- ただし、通信の多さを抑えるため、1プレイヤーあたりの送信頻度に上限を設ける（`markerSendMinIntervalTicks`）
- 他のプレイヤーが範囲に入ってきたとき（近づいたとき）は、その人の現在の弱点の状態を、新しく入ってきたプレイヤーへ送る
- 消すとき: 「消えた」通知を受けたとき、一定時間なにも届かなかったとき、相手が切断したとき、範囲の外に出たとき

### 8.5.3 対象
- 最初は、**採掘の弱点だけ**にする（左クリックの弱点）
- 成長・機械（右クリックの弱点）は、後から同じ仕組みで追加できる

### 8.5.4 設定
- 表示のオンオフ、色、透明度は、各自のクライアントの設定（§3.3）
- 転送する範囲と送信頻度の上限は、サーバーの設定（§9）。範囲を 0 にすると、転送しない
- 設定は、Forge の設定画面で変える（統計画面のボタンから開ける。§3.5）。色や透明度は、試聴の要らない設定なので、統計画面には置かない

## 9. 設定項目（すべてサーバーの値。追加分）

| キー | 初期値 | 説明 |
|---|---|---|
| `hitsPerRepair` | 5 | 何ヒットごとに、耐久を回復するか |
| `repairPerStep` | 1 | 1回で回復する耐久 |
| `milestones` | 100, 777, 1000, 10000 | 節目の数字 |
| `milestoneXp` | 10, 77, 30, 100 | 節目ごとの経験値（節目の順に対応。仮置き） |
| `milestoneRepair` | 10, 77, 30, 100 | 節目ごとの耐久回復量（節目の順に対応。仮置き） |
| `growthTicksPerHit` | 5 | 成長ヒット1回あたりの、余分な `randomTick` 回数（仮置き） |
| `growthMinHitIntervalTicks` | 要調整 | 成長ヒットを受け付ける最小間隔 |
| `machineBoostMultiplier` | 4.0 | 機械ヒット時の `update()` の倍率 |
| `machineBoostDurationTicks` | 4 | 機械の加速が続く時間（tick） |
| `machineMinHitIntervalTicks` | 要調整 | 機械ヒットを受け付ける最小間隔 |
| `excludedBlocks` | 宝箱、トラップチェスト、エンダーチェスト、エンチャントテーブル、ビーコン | 機械の加速の対象外にするブロックの登録名 |
| `markerShareRange` | 16 | 他のプレイヤーの弱点マークを転送する範囲（ブロック）。0 で転送しない |
| `markerSendMinIntervalTicks` | 要調整 | 弱点マークの状態を、1プレイヤーあたり送る最小間隔（tick） |

`milestones` / `milestoneXp` / `milestoneRepair` は、同じ順番で対応させる（節目の数と、量の数が合わないときは、足りない分を 0 として扱い、ログに警告を出す）。

## 10. 通信の追加（案）

パケットの中身を変えるので、1.0.x 以前とは通信できなくなる。README の更新履歴に書くこと。サーバーと全員のクライアントを、1.1.0 に同時に更新する。

| 方向 | 内容 |
|---|---|
| サーバー → クライアント | 設定値の送信（ログイン時、変更時） |
| クライアント → サーバー | ヒット通知（種類: 採掘 / 成長 / 機械。位置を含む）。既存の `HitMessage` を拡張してよい |
| クライアント → サーバー | 統計の要求（統計画面を開いたとき） |
| サーバー → クライアント | 統計の送信 |
| サーバー → クライアント | 節目の通知（達成した節目の数字） |
| クライアント → サーバー | 自分の弱点マークの状態（出た / 動いた / 消えた）。§8.5 |
| サーバー → クライアント | 他のプレイヤーの弱点マークの状態（範囲内のプレイヤーへの転送、近づいたときの現在状態、消えた通知）。§8.5 |

- 他のプレイヤーへのヒット音の転送（`OtherHitMessage`）は、採掘ヒットだけ（案）
- 成長・機械のヒット音は、自分の音として、既存のヒット音の仕組みを使う（案）

## 11. README に書くこと

- 統計と設定が、サーバー側になったこと（累計が、ワールド・サーバーごとになること）
- 機械の加速の**リスク**（§7.6）。ワールドのバックアップを取ってから使うこと
- 問題が出た機械は、`excludedBlocks` に登録名を足せば、対象外にできること
- 素手でしゃがんで右クリックすると、機械の GUI が開かなくなること（しゃがまずに右クリックすれば開く）
- 専用サーバーでは、クライアントの Mods メニューの設定は使われないこと（ヒット音の楽器と音量、他のプレイヤーのマークの表示は、各自の設定なので使われる）
- ヒット音の楽器と音量を、統計画面（K キー）のサウンドの部品で変えられ、試聴できること。他のプレイヤーのマークの表示は、Forge の設定画面（統計画面のボタンからも開ける）で変えられること
- 他のプレイヤーの弱点マークが、色を変えて見えること（見えるだけで、当たり判定はない）。範囲はサーバーの設定 `markerShareRange` で、0 にすると見えなくなること
- 通信内容が変わったので、サーバーとクライアントを、1.1.0 に同時に更新すること（1.0.x とは接続できない）
- 「最新版」の行を 1.1.0 にし、「更新履歴」に 1.1.0 を追加する（§0）
- README に「バージョンの方針」の節を追加する（下の案）
- 「インストール」の「どこに入れるか」と「更新するとき」の記述を、新しい方針に直す。今の README には、「同じバージョンの jar を入れる」「バージョンが違うと接続できない」と書いてある。これを、「同じマイナーバージョン（1.1.x 同士）なら、パッチが違っていても接続できる。マイナーが違うと接続できない」に直す
- 「設定」の節を、§3 に合わせて直す。今の README には、「設定はサーバーとクライアントの間で同期されません」と書いてある。これを、「サーバーの値が正で、接続時にクライアントへ送られる。各自の設定として残る項目は、ヒット音の楽器・音量と、他のプレイヤーのマークの表示・色・透明度」に直す

**README の「バージョンの方針」の案**

```markdown
## バージョンの方針

- **パッチ**（1.1.0 → 1.1.1）: 機能の追加や、不具合の修正です。通信内容や保存データの形式は変えないので、同じ 1.1.x の間は、サーバーとクライアントのパッチ版が違っていても接続できます。
- **マイナー**（1.1.x → 1.2.0）: 互換性が壊れる変更です（通信内容の変更、古い版では読めない保存データの変更、設定キーの削除や意味の変更）。マイナーが違うと接続できないので、サーバーと全員のクライアントを、同じマイナーに更新してください。
- 迷ったときは、マイナーを上げます。
```

**README の更新履歴の案（1.1.0）**

```markdown
### 1.1.0

- 統計と設定を、サーバー側で管理するようにした（統計はワールド・サーバーごと。設定はサーバーの値が正）
- 統計画面（K キー）に、ヒット音の楽器・音量の変更と試聴、Forge の設定画面を開くボタンを追加した
- 5ヒットごとに、手に持っているツールの耐久が1回復するようにした（設定で変えられる）
- 100 / 777 / 1000 / 10000 ヒットの節目で、祝いの演出と、少量の経験値・耐久回復を追加した
- 作物・苗木に、右クリックしっぱなしで弱点が出て、当てると成長が促進されるようにした
- 機械に、しゃがんで右クリックしっぱなしで弱点が出て、当てると加速されるようにした（機械 MOD によっては不具合の原因になることがある。ワールドのバックアップを取ってから使うこと）
- 他のプレイヤーの弱点マークが、別の色で見えるようにした
- 通信内容が変わったため、1.0.x とは接続できない。サーバーとクライアントを、1.1.0 に同時に更新すること
- 今後は、同じ 1.1.x の間は、パッチ違いでも接続できる（バージョンの方針を参照）
```

## 12. 実装時に確認すること

- クライアントとサーバーの設定の同期の仕組み（現行は `@Config` で、同期していない）
- プレイヤーごとの統計の保存先（プレイヤーの永続データ、またはケイパビリティ）と、死亡時に引き継がれること
- 右クリックを押しっぱなしにしたとき、`RightClickBlock` が一定間隔で繰り返し発火するか
- 通常の右クリック動作を、クライアントとサーバーの両方で、弱点が出ている間だけ止められるか。クライアント側で止めたときに、サーバーへ右クリックの情報が届くか
- 今の弱点の配置が、1ブロックの面（正方形）を前提にしているか。作物のような低い当たり判定でも動くか（§6.5）
- サーバーでの「壊した」判定（破壊イベント）と、「弱点を当てて壊した」の判定
- `update()` を余分に呼ぶタイミング（サーバーの tick のどこで呼ぶか）
- クライアントとサーバーの、ブーストの結果が一致すること（`CLAUDE.md` の「サーバー側のブーストは時間枠ではない」の注意を守る）
- 試聴で、任意の音量・高さ・楽器の音を、距離なしで鳴らす方法。バニラの「プレイヤー」の音量設定が掛かること
- 統計画面のサウンドの部品の値が、`weakspot.cfg`（`@Config`）に保存され、Forge の設定画面と食い違わないこと（保存や再読み込みの呼び出しが必要かも確認する）
- 統計画面のボタンから、Forge の設定画面を開く方法（Mods メニューの「設定」ボタンと同じ呼び出しが使えるか）。閉じたときに統計画面へ戻り、値を読み直せること。うまくいかない場合は、自前で設定画面を組み立てて開く
- 今の自分の弱点マークの色（他のプレイヤーのマークの初期色 `#3FA9FF` と区別できるか。§15）
- 他のプレイヤーの弱点マークを描く方法（今の描画の仕組みを、他のプレイヤーの分にも使い回せるか）
- 他のプレイヤーが範囲に入ってきたときに、そのプレイヤーの現在の弱点の状態を送る方法（プレイヤーの追跡が始まったときのイベントなど）

## 13. 実装の進め方（案）

リリースは、1.1.0 にまとめる（§0）。次は、開発の進め方の目安で、順番は入れ替えてよい。

1. 統計と設定のサーバー化、耐久回復（5ヒットごと）、節目の演出と報酬、統計画面のサウンドの部品（ヒット音の変更と試聴。サウンドの部品はサーバーと関係ない）
2. 作物・苗木の成長促進
3. 機械の加速
4. 他のプレイヤーの弱点マークの表示（色の変更まで。形の変更は、1.1.0 の後のバージョン）

通信内容が変わるため、動作確認では、サーバーと全員のクライアントが、同じビルドである必要がある。外に出すのは、すべてがそろってから、1.1.0 として出す（§0）。

## 14. スコープ外

- ヒットごとの経験値
- コンボ、連続ヒットのボーナス
- 節目の、他のプレイヤーへの発表
- 本格的な不正対策（ヒットの検証は、今の範囲にとどめる）
- 機械の許可リスト
- ヒット音の音階の上がり方の変更（音階の種類、繰り返しのオン・オフなど）。変えられるのは楽器と音量だけ
- 他のプレイヤーの弱点マークの**形**の変更（1.1.0 では色だけ。後のバージョンで追加する。候補: 中抜きの輪、ひし形、四角）
- 他のプレイヤーのヒットの瞬間のエフェクト（弱点マークの常時表示とは別。後から追加できる）
- 成長・機械の弱点マークの、他のプレイヤーへの表示（最初は採掘だけ）
- プレイヤーごとにマークの色や形を変えること（必須ではない）
- 1.12.2 以外のバージョン（1.7.10 への移植は、MVP 後）

## 15. 未決事項（仮置きで進めてよい）

- `growthTicksPerHit` などの、成長・機械の数値（遊びながら調整）
- 節目の経験値・耐久回復量の具体的な数字（§9 の値は仮置き）
- 777 を、1回きりにするか、777の倍数ごとに繰り返すか（初期は1回きり）
- 機械の加速中に例外が出たとき、その機械のクラスを自動で対象外にするか（サーバー全体が止まるのを避ける保険。推奨だが、必須ではない）
- 対象外の初期リストに、モブスポナーなどを加えるか
- 他のプレイヤーの弱点マークの初期色（水色 `#3FA9FF`）。自分のマークの色が青系なら、別の色（緑 `#3DDC84`、紫 `#B26BFF`、黄 `#FFD23F` など）に変える
- 他のプレイヤーの弱点マークの、初期の透明度と、送信頻度の上限の具体的な数字（遊びながら調整）
